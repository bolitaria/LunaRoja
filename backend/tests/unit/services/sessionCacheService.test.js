const service = require('../../../src/services/sessionCacheService');

describe('sessionCacheService service', () => {
  test('debe exportar una función o un objeto', () => {
    expect(service).toBeDefined();
  });
});
