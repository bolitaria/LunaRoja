const service = require('../../../src/services/migrationService');

describe('migrationService service', () => {
  test('debe exportar una función o un objeto', () => {
    expect(service).toBeDefined();
  });
});
